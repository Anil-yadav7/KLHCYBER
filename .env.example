# ==============================================================================
# BREACHSHIELD ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# INSTRUCTIONS:
# 1. Copy this file into a real environment file: `cp .env.example .env`
# 2. Fill in all keys with your real authentication values
# 3. NEVER commit your real `.env` file to version control (Git)
#
# SETUP HELPERS:
# * HIBP API Key: Register at https://haveibeenpwned.com/API/Key
# * ENCRYPTION_KEY: Generate a valid symmetric key via Python:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ==============================================================================


# ── Application ──────────────────────────
# Descriptive title of the application instance
APP_NAME=BreachShield API
# Active semantic versioning instance
APP_VERSION=1.0.0
# Base API URL used by the frontend dashboard to communicate with backend
API_BASE_URL=http://localhost:8000/api/v1
# Console logging verbosity (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO


# ── Database ──────────────────────────────
# Connection string (Use SQLite locally, switch to postgresql:// in production)
DATABASE_URL=sqlite:///./breachshield.db


# ── HIBP API Configuration ────────────────
# Personal authorization key for HaveIBeenPwned endpoints
HIBP_API_KEY=your_hibp_api_key_here
# Seconds to sleep between heavy API calls to obey fair-use rate limiting
HIBP_RATE_LIMIT_SECONDS=1.6


# ── Email (SendGrid) ──────────────────────
# Full SendGrid API Secret Key
SENDGRID_API_KEY=your_sendgrid_api_key_here
# The exact authorized sender address configured in your SendGrid portal
FROM_EMAIL=alerts@yourdomain.com
# The human-readable string displayed in the 'From' column of the email client
FROM_NAME=BreachShield Security


# ── SMS (Twilio) ─────────────────────────
# Twilio Account SID identifier
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
# Twilio Account Authentication Token
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
# E.164 formatted registered Twilio phone number
TWILIO_FROM_NUMBER=+15551234567


# ── AI / Anthropic Claude ────────────────
# Secret API key for Anthropics Claude language models
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# The precise model tag to instantiate
CLAUDE_MODEL=claude-3-5-sonnet-20241022


# ── Redis & Celery ───────────────────────
# Connection URL pointing to working Redis broker instance
CELERY_BROKER_URL=redis://localhost:6379/0
# Connection URL pointing to Redis database used for Celery states
CELERY_RESULT_BACKEND=redis://localhost:6379/1


# ── Security ─────────────────────────────
# Root application cryptographic secret key (Minimum 32 random characters)
SECRET_KEY=generate_a_long_random_secret_key_here
# Fernet-compatible symmetrical database encryption key (Use helper command above to generate!)
ENCRYPTION_KEY=your_base64_generated_fernet_key_here
